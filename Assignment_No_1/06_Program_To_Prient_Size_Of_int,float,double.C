#include<stdio.h>
#include<conio.h>

int main()
{
    printf("\n\n char size is   : %d byte",sizeof(char));
    printf("\n\n int size is    : %d byte",sizeof(int));
    printf("\n\n float size is  : %d byte",sizeof(float));
    printf("\n\n double size is : %d byte",sizeof(double));

    getch();
    return 0;

 }
